server {
    listen 3000;
    root /usr/share/nginx/html;
    index index.html;

    # Vue Router: отдаём index.html для всех маршрутов
    location / {
        try_files $uri $uri/ /index.html;
    }
}
